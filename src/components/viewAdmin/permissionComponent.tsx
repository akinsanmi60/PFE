import CustomButton from '@shared/Button';

function PermissionComponent() {
  return (
    <CustomButton className="text-primary-white">
      Assign Permission
    </CustomButton>
  );
}

export default PermissionComponent;
